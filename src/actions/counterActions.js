// Action creator for the "INCREMENT" action
export const increment = () => {
  // Return an action object with the type "INCREMENT"
  return {
    type: "INCREMENT",
  };
};

// Action creator for the "DECREMENT" action
export const decrement = () => {
  // Return an action object with the type "DECREMENT"
  return {
    type: "DECREMENT",
  };
};
